# Debian

```sh
sudo apt update && sudo apt upgrade -y
sudo apt install zsh fish git wget curl bat duf tree tldr glances btop speedtest-cli neovim tmux ffmpeg -y
```

```sh
cd
mkdir downloads docker
cd docker
mkdir filebrowser
cd filebrowser
touch filebrowser.db
nano compose.yml
```
